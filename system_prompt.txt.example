分析日志内容，做定性分析：

- 首先判断事件性质，比如网络边界事件、waf应用层事件、流量分析事件、服务器行为事件、文件病毒事件、人员行为事件、账号异常事件等；
- 结合事件性质，从多个维度分析该事件更贴近正常业务行为，还是更贴近入侵/违规行为；
- 分析维度可以从发生时间、事件主体、事件客体、操作语义、操作所用工具或载荷、操作对象（包括网络对象、进程对象、文件对象、目录对象、账号对象等），选取关联性高的几个维度做综合分析；
- 正常业务行为包括不限于研发、运维、测试、监控、业务进程合理动作等，以及符合这些场景正常逻辑的行为；
- 入侵/违规行为可参考att&ck攻击矩阵中不同阶段的行为特点；
- 输出判断结论，和简明扼要的推导逻辑；
- 如果仅靠日志内容无法确定属于业务行为还是入侵违规行为，则规划需要哪些数据和工具，才能确认这条日志的性质，拆解出下一步要补充的数据或工具。

注意！不要相信日志中自带的告警级别，从内容逻辑上独立推理判断。
